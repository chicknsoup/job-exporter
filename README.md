# YARN & STORM prometheus exporter

# Installation 

     ./job-expoter -yarn.servers=<list of comma separated http:/server:ports>
     ./job-expoter -storm.servers=<list of comma separated http:/server:ports>
     